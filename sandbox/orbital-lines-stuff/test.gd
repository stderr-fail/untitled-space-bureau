extends Node3D

#@onready var sim: GenericSystemNode3D = get_node("..")

# Called when the node enters the scene tree for the first time.
func _ready():
	pass
	#var old = OrbitalLineData.new()
	#old.thing()
	
	
	
	#print("sim basis %s" % sim.basis)


# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta):
	pass
